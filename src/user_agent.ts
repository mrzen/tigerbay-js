import { VERSION } from './version'

/**
 * User-Agent header value
 * 
 * @todo Re-generate this when package is built
 */
export const USER_AGENT = `tigerbay-js (+https://github.com/mrzen/tigerbay-js v${VERSION})`